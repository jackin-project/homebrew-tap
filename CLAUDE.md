# CLAUDE.md

See [AGENTS.md](AGENTS.md) for shared agent instructions.

This repo is a **public Homebrew tap**. Most commits come from automation (CI in `jackin` bumps the preview formula); manual edits warrant extra scrutiny. Before every manual commit, run the pre-commit checks in AGENTS.md — `brew fetch` to verify url/sha256 integrity, plus a defense-in-depth credential scan.

This repository uses `main` as its primary branch.
